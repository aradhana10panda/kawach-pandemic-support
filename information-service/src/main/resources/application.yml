server:
  port: 8088

spring:
  application:
    name: information-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8076/eureka
    register-with-eureka: true
    fetch-registry: true
